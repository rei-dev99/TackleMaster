<% @rods.each do |rod| %>
  <li class="w-80 flex items-center gap-x-3.5 py-2 px-3 rounded-md text-sm text-black" role="option" data-autocomplete-value="<%= rod.id %>" data-autocomplete-label="<%= rod.name %>">
    <%= rod.maker %>
  </li>
<% end %>
